# 改造台账

按时间记录对 DSH 做的改造、验证情况和踩到的死路。新条目往后追加,旧条目不改。

| 日期 | 项目 | 变更 | 验证 |
|---|---|---|---|
| 2026-08-22 | `tools/update-dsh.mjs` | 新增:受管安装(脱离 npx 缓存)、备份 → 预取 → 冒烟 → 配置 diff → 切换 → 回滚;支持多 dist-tag 通道 | 0.1.0-rc.6 → 0.1.1-rc.2 实跑通过(备份、diff、切换、回滚均验证) |
| 2026-08-22 | `tools/migrate-dsh.mjs` | 新增:整套工作台打包与还原(数据、配置、插件、远程清单),还原时重写绝对路径并重装引擎 | 全流程验证:打包 → 新 home 还原 → 引擎重装 → 真实启动成功 |
| 2026-08-22 | `plugins/dsh-plugin-amend` | 新增:用 compaction 同款 surface-replace 机制修改历史消息,日志保持 append-only | 表面层替换实测生效(`surface.nodes` 由 `[0,1]` 变为 `[0,3]`,原事件保持 append-origin) |
| 2026-08-25 | `dsh-remote/supervisor.sh` · `install-supervisor.sh` | 新增:launchd 常驻,登录自动恢复隧道/实例,面板崩溃自动拉起 | 杀死面板 5 秒内自动重启;sim 实例随启动恢复;修复 launchd 环境缺 node 的问题 |
| 2026-09-03 | 全量 | 引擎升级 0.1.1-rc.2 → 0.1.2-alpha.5(alpha 通道) | 配置 diff:消失 3 个 id(均为内部重构,未被 patch 引用)、新增 13 个;真实启动通过 |
| 2026-09-03 | `tools/update-dsh.mjs` | 新增 `prune` 子命令清理旧版本;`status`/`check` 改为显示全部 dist-tag(此前看不到 alpha 通道) | 实跑通过;prune 正确保护 current 与回滚目标 |
| 2026-09-03 | `dsh-remote/cli.mjs` · `panel.mjs` | 修复 PID 复用导致的"假活"(重启后旧 PID 被新进程占用,实例死了却显示在运行) | 存活判断改为 PID + 端口双确认;`disconnect` 对复用 PID 不再误杀;用真实过期记录验证 |
| 2026-09-03 | `skills/gpu-partition` | 新增:远程 GPU 工作分区模板(ssh 别名 + 免密接入 + rsync/远程执行规范) | 远程读写、rsync 干跑、真实读取项目目录均通过 |
| 2026-09-03 | 仓库 | 建立本仓库,整理工具/面板/插件/技能为留档 | 已核对:不含服务器地址、凭据或本机路径 |
| 2026-09-10 | `plugins/dsh-fs-sftp` | 新增:`ctx.fs` 的 SFTP provider(继承 `FileSystem`,实现 11 个契约方法;原子写 = 同目录 staging + rename)。工作区注册表的本机校验是硬约束,用「本机空挂载点目录 + 前缀映射」绕过 | agent 用原生 `read`/`write` 直读直写远程文件,内容与独立 SFTP 读取逐字节一致;写入的文件出现在远程,本机挂载点始终 0 字节、0 文件 |
| 2026-09-10 | `plugins/dsh-subprocess-sftp` | 新增:继承 `LocalSubprocessRuntime`,**只覆盖 `spawn`**,把 argv/cwd 翻译成一条 ssh 调用(收集/spill/超时/终止全部复用官方实现)。一个 provider 同时解决两个工具:`bash`(因为 `LocalBashExecutor` 本身是 `ctx.subprocess` 的消费方)与 `glob`/`grep`(走 `ctx.subprocess.spawn([rgPath,…])`) | `uname -sr` 返回远程 Linux 内核(本机是 Darwin);glob 命中数与远程 `find` 一致;grep 在 9 个远程 `.py` 文件里命中 —— 这些文件本机不存在 |
| 2026-09-10 | `plugins/dsh-bash-sftp` | 新增:继承 `LocalBashExecutor`,只如实汇报 `sandboxMode = danger-full-access`(远程以 root 运行、本机不施加文件沙箱)并把默认 workdir 指向挂载点 | 不汇报则 `dsh-permission-presets` 在构造期直接抛错、整个组合启动失败;配套在 permission 预置表补一条 `(danger-full-access, ask)` 后正常启动 |
| 2026-09-10 | `plugins/dsh-directory-picker-sftp` | 新增:远程版目录选择器(`{kind:'browse', list, createDirectory}`)。列举远程目录时把每个目录在本机物化成空占位骨架,于是「Add workspace」选中的路径天然满足注册表的本机校验;新建文件夹同时在远程与本机建 | 列出远程 `/root` 下 24 个目录、面包屑显示 `server:/root › <数据盘>`;新建目录在远程与本机各建一份(远程已独立确认);重复建报 `directory-exists`、非法段名报 `directory-create-failed`;本机骨架 0 字节 0 文件 |
| 2026-09-10 | `dsh-remote/workbench.mjs` · `panel.mjs` · `panel.html` | 新增:面板一键建「远程挂载工作台」——建分区 home/profile → 装 4 个 provider → 写映射配置 → 建挂载点 → 继承凭证/设置/技能 → 注册清单 → 启动并验证存活;分步回报,失败自动回滚半成品 | HTTP 全流程 8 步全绿;新建出的工作台三接缝实测全部落在远程 |
| 2026-09-10 | `dsh-remote/panel.mjs` · `supervisor.sh` | 修复两处环境问题:①launchd 的 PATH 缺 `$HOME/.dsh/bin`,建工作台时报 `pnpm not found`;②direct 实例启动输出原为 `stdio:'ignore'`,实例崩溃无从排查,改为落 `.state/boot-<name>.log` | 补 PATH 后安装步骤通过;启动日志实测用于定位一次真实崩溃(端口开放 ≠ 启动成功,须确认进程存活) |
| 2026-09-11 | `plugins/dsh-fs-sftp` | 补 `readByteRange(target, {offset,length}, signal)`:0.1.5 起 fs 契约由 12 个抽象方法增至 13 个,新增此方法供工作区文件树/文档预览做分页读。语义对齐官方 `readByteWindow`(先确认常规文件;`length===0` 返回空;窗口越界返回空**不报错**) | 用远程 `head -c` / `dd` 的 md5 独立比对 3/3(含一个被文件末尾截断的窗口);边界(长度 0、越界、恰好末尾)全对;错误码为 `FS_NOT_REGULAR_FILE`/`FS_IO_ERROR`;并在 0.1.5 契约下复验 |
| 2026-09-11 | 全量 · 引擎升级 | 0.1.2-rc.1 → 0.1.5-rc.2。该版本新增右侧栏 + 工作区文件树 + 文档预览 + 外部打开(新增 8 个 Loader 行),`tool-str-replace-editor` 行消失。**上游侧栏文件树走 `ctx.fs`** —— 因此远程 fs provider 让它自动显示远程目录树,自研的 `dsh-remote-ui` 抽屉(走面板 HTTP API)随之退役 | 旧版 `--dump-config` 对比新增/消失行;测试分区实跑 canary:bash 返回远程 Linux 内核、glob/grep 与远程真值一致;**上游重写的 `LocalSubprocessRuntime.spawn`(新增 cgroup scope / Windows job 进程约束)未打断我们的 `spawn` 覆盖**;我们禁用的 5 个行 id 全部仍在(patch 不会静默失效);升级后 web 实例的客户端插件图确认 `ui-sidebar-files`/`ui-sidebar-right`/`workspace-files` 与自研 `directory-picker-browse` 同挂 |
| 2026-09-11 | `plugins/dsh-fs-sftp` | **修复两个真 bug**(都由端到端实测暴露,均为「新建能过、覆盖必挂」类):①`writeAtomic` 用普通 `SSH_FXP_RENAME`,而 OpenSSH 按 SFTP v3 规范**拒绝覆盖已存在文件**(报 code 4 `Failure`)—— 导致 `editText` 与覆盖式 `writeText` 全程不可用;改为优先 `ext_openssh_rename`(posix-rename 扩展,真正的 `rename(2)`,原子且可覆盖),服务端不支持时退回「先删再改名」。②`editText` 的版本守卫读的是 `existing.version`,而 `probe()` 返回**原始 ssh2 Stats 对象**(没有该字段)→ `undefined !== expected.version` 恒真,**只要调用方带版本守卫就必报 `FS_STALE_VERSION`**;改用 `versionOf(existing)`(与 `writeText` 一致)。另:发布失败时清理暂存文件 | 直测:覆盖式写入 / 单处编辑 / 全量替换 / 版本守卫(新鲜成功 + 过期报错)全通过;端到端由 agent 用 `edit` 工具改远程文件,会话日志 `changed since` × 0、`isError` × 0,远程独立核对内容正确且无 `.dsh-tmp-*` 残留 |
| 2026-09-11 | `dsh-remote/lib/sftp.mjs` | 同一根因的另一处:`rename` 也改为 posix-rename 优先(影响 `rw.mjs mv` 覆盖已存在文件) | `rw.mjs mv` 覆盖目标成功、源消失、目标内容正确 |
| 2026-09-11 | `plugins/dsh-fs-sftp` | **新增:三档权限在文件工具上的执行**(`read-only` / `workspace-write` / `danger-full-access`)。契约规定 `writeText`/`editText` 的第 5 个参数就是 per-call `sandboxPolicy`(*"由 sandboxing backend 据此围栏,bare backend 忽略"*),据此加 `checkedTarget` 围栏,与官方 `dsh-fs-sandbox` 同构:read-only 拒绝一切写入;workspace-write 只允许写在 `workspaceRoot` 之内;full 放行;拒绝抛 `FS_SANDBOX_DENIED`。另加 `sandboxMode` getter(报 `ctx.sandboxPolicy.defaultMode`),模型侧文件工具才会如实显示"可升级到更宽档位" | 策略矩阵实测:read-only 对工作区内/外与 `editText` 全部拒绝且**不碰网络**;workspace-write 工作区内放行(本机挂载点与远程 `/root/...` **两种拼写都正确识别**)、工作区外拒绝、挂载点根(工作区之父)拒绝;danger-full-access 放行;无策略不围栏。组合层:预置表经 `permission-presets` 一致性断言,真实启动无报错 |
| 2026-09-11 | `plugins/dsh-bash-sftp` | permission 预置表恢复三档并**如实命名**:`只读(文件工具)` / `工作区可写(文件工具)` / `完全放开`,外加原默认档 `远程(默认:文件工具也放开)`。描述里写明这三档只约束文件工具、**shell 命令不受其约束**(远程命令由审批策略兜底)—— 因为把 shell 也纳入需要在远程机上真搭一层沙箱(bwrap 之类),没搭就往上报 workspace-write 等于撒谎 | 组合后的预置表经 `--dump-config` 核对;真实启动不再触发 `permission-presets` 的 misconfiguration 断言 |
| 2026-09-11 | `plugins/dsh-fs-sftp` · `dsh-directory-picker-sftp` | 修复:`withTransport` 里**建连不在 try 内**,连接失败会绕过 `mapRemoteError` 直抛原始 net 错误,上层只见 `code=UNKNOWN`。现纳入映射,连接失败给出可操作文案 | 实测:远程不可达时回合错误从 `UNKNOWN: connect ECONNREFUSED …` 变为 `cannot stat "…/.git": 远程连接失败(…);检查机器是否在线、SSH 端口是否变了` |
| 2026-09-11 | `dsh-remote/repoint.mjs`(新增)· `workbench.mjs` · `panel.mjs` | 支持「克隆实例后只换端点」:云上克隆出的新机数据盘/系统盘与旧机一致,只有 host 与端口变。新增 `repoint` 一次改全端点(散在三处,少改一处会得到"文件工具能用、bash 用不了"的半通状态:清单 `conn`、各分区挂载配置、可选 `~/.ssh/config`),支持 `--dry-run`、自动备份。`workbench.mjs` 生成挂载配置时从源条目推导 `sshTarget=user@host` + `-p 端口` + `StrictHostKeyChecking=accept-new` —— 新建工作台**不再要求写 ssh 别名**,也不会因未知 host key 卡住首条 ssh 命令 | 实机走通:克隆机换端点(主机与端口都变)后,fs 层读已知文件 md5 与旧机**逐字节相同**、`readByteRange` 同窗口一致;三接缝 `bash` 返回新机 hostname 与内核(旧 5.15 / 新 6.2)、`glob` 342 文件、`grep` 命中的 9 个文件与旧机完全一致;`--dry-run` 验证不写盘(改动前后文件指纹一致) |
| 2026-09-13 | `dsh-remote/repoint.mjs` 实战 · 3090 工作台换实例 | 把 3090 从旧实例切到新租的实例(**host 与端口都变**,不是端口微调)。按 repoint 的三处一次改全:`servers.json` 的 `conn`(fs provider / `rw.mjs` / 目录选择器)、`sim-homes/<分区>/remote-mount.json` 的 `sshTarget`+`sshExtraArgs`(subprocess provider,即 bash/glob/grep)、`~/.ssh/config` 的 `Host` 块(`--ssh-config`,供手动 ssh)。`remoteRoot`/`localRoot` 都没变,所以工作区登记一个字不用改 —— 这正是「克隆机」场景。顺手删掉早先试接缝留下的孤儿测试分区 `sim-homes/fs-sftp-test`(无对应机器条目;已打包留底到 `.state/trash/`)。`~/.ssh/config` 在工作区之外被 workspace-write 沙箱以 EPERM 挡下 → 前两处先落盘,第三步按规则重试同一条命令并申请一次性放行后完成 | ①克隆期用 **`df` 而非 `du`** 每分钟采样(不 walk 目录树、不干扰克隆 I/O),判据「两个目标工作区出现且有内容 + 占用连续多次不变」;②克隆完成独立核实:写入已停(15 秒两次差 0 MB)、`du -sh` 总计与 `df` 已用自洽、各项目体积与旧机账面**逐项一致**(不记具体数值)、两个登记工作区都在且非空;③**两条接缝的连接路径都直接验过,不必等一次 agent 回合**:fs 那条用 `rw.mjs ls`(与 fs provider 读同一份 `servers.json`、走同一个 `lib/sftp.mjs`)列出工作区 35 项;subprocess 那条按 provider 原样的参数拼出 `ssh <sshExtraArgs> <sshTarget>`,返回新机 hostname 与内核、`cd` 进得去、`rg` 已就位;④实例重启后 boot 日志 error 行 0,客户端插件图里官方文件树/右侧栏在、已卸载的 `dsh-remote-files` 为 0;⑤面板侧 `whoami` 报出新 host,两个 roots 与 `focus` 映射不变;⑥本机挂载点仍 0 文件 / 0B(零拷贝不变量没破) |

## 走不通的(留档以免重踩)

| 日期 | 尝试 | 结果 |
|---|---|---|
| 2026-09-03 | macOS 26 + Apple Silicon 上把远程目录做成真·系统挂载 | 三条路均受阻:WebDAV 挂载注册成功但访问 EPERM;SMB(guest/用户认证、回环/局域网 IP、标准/非标准端口)连接到不了服务端;FUSE-T + sshfs 报 `fuse: mount failed error: -1`。上游 issue 佐证:macOS 26 上 FSKit 扩展启用失败(#104)、26.6.2 上 SMB 连接不稳定(#113)、经 fuse-t 挂载触发 Apple NFS 内核 bug(#109)。结论:看远程文件用 VS Code Remote-SSH,不做系统挂载 |
| 2026-09-03 | 在本机与仓库之间做软链(单一数据源) | 放弃:本机是持续使用的生产环境,不应依赖仓库目录存在与否;改为解耦(仓库只是发布副本) |
| 2026-09-10 | 禁用 `dsh-host-directory-picker-auto` 后只补自己的宿主后端 | 「Add workspace」按钮消失。该包不是普通 provider,而是**成对挂载**适配器:启动时把宿主后端与客户端界面作为两个 Loader 条目一起挂(`BACKEND_PACKAGES` + `SURFACE_PACKAGES`),禁用它等于两面一起删。正解按官方注释「直接组合成对」:自己的后端 + `@deepseek-ai/dsh-client-ui-directory-picker-browse` |
| 2026-09-10 | ssh 连接复用的 ControlPath 放在 `$DSH_HOME/dsh-remote/.ssh-cm/` | Unix domain socket 的 `sun_path` 上限 104 字节,分区 home 路径长必然溢出(`ControlPath too long`);且 ssh 还会在该路径后追加 `.` + 16 位随机后缀,长度守卫必须按**展开后**计算。改用 `/tmp/dsh-ssh-cm/%C`;复用生效后单次 ssh 从 0.58s 降到 0.08s |
| 2026-09-10 | 用 `curl` 直接抓 dsh web 实例页面做程序化验证 | token 是一次性的,且 `dsh web` 默认会自动打开浏览器把 token 消费掉;必须先 `--no-open` 起实例,再用 `?token=` 换 cookie(303 + `Set-Cookie`)带 cookie jar 跟进去才能拿到页面 |
| 2026-09-11 | 在测试分区里用新引擎版本实测 | 踩雷:`$DSH_HOME/profiles/node_modules` 若是**指向另一个 home 的符号链接**,启动时会按「治愈到当前运行的安装」的逻辑**穿透改写对方** —— 等于拿新版本跑一次测试就污染了生产环境的模块解析。测试分区必须先把它换成独立目录(启动时自建) |
| 2026-09-11 | 排查三个实例同时不可达 | 白查一场:服务是**用户自己在面板上「全部断开」关的**。事后看判据一直摆在眼前 —— 三个同时消失而 supervisor/panel 都活着、启动日志只有启动行没有堆栈(SIGTERM 而非崩溃)、`tunnels.json` 变成空表(正是 disconnect-all 的正常输出)。**看到空表应先问「谁主动关的」,再往故障方向查** |
| 2026-09-11 | 我自己验证 fs provider 的方式 | 上面两个 bug 都躲过了初测,原因是**只测了「新建」和「不带守卫」**:写文件只测 create、编辑只测「不带 expected」与「故意过期」两种。而工具层实际走的是**覆盖写 + 新鲜版本守卫** —— 恰好是没测的两条。教训:provider 的写入路径必须成对测「新建 / 覆盖」,守卫必须成对测「新鲜(应成功)/ 过期(应报错)」 |
| 2026-09-11 | 两轮 canary 排查同一文件 | 旧代码的进程没退就启动了新一轮,两个 agent 抢同一个远程文件,会话日志互相污染,一度让我误判「修复无效」。**在排查并发写同一目标的问题前,先确认没有残留进程**(`pgrep -fl`),否则结论不可信 |
| 2026-09-11 | 读 0.1.5 的会话日志做取证 | 会话日志换成 `session.v3.jsonl.zstd`,且是**多帧 zstd**(每批 append 一个 frame):`zstdDecompressSync` 与 `createZstdDecompress` 都只解第一帧(实测 113KB 只解出 200B)。要按魔数 `28 b5 2f fd` 切分后逐帧解压,才能拿到完整的工具调用与结果 —— 这是排查「工具到底报了什么」最硬的证据 |
| 2026-09-11 | 远程机不可达时的行为 | 记录一个运营事实:**远程一断,挂载型分区连一个回合都跑不起来** —— 回合启动阶段有组件经 `ctx.fs` 探测 `<工作区>/.git`(VCS 上下文一类),连接失败直接终止该回合。所以「机器是否在线 / SSH 端口是否变了」是这类分区的第一检查项;错误文案已改为可操作提示 |
| 2026-09-11 | 三档权限的诚实边界 | 远程世界的 `read-only` / `workspace-write` **只约束文件工具**(`read`/`write`/`edit`);`bash` 命令不经过 fs 接缝,远程命令本身不受它们约束,由审批策略兜底。要真正约束 shell 必须在远程机上搭一层沙箱(你的机器:bwrap 可装、root、userns 已开),**没搭就不要谎报 workspace-write** |
| 2026-09-11 | `dsh-remote/panel.mjs` · `panel.html` | 新增**文件管理器 UI**:每台服务器卡片上的「📁 文件」打开双栏管理器 —— 左栏目录树(点击进入 + 面包屑)、右栏查看/编辑;支持增删改查(新建文件/文件夹、重命名、删除)、**拖拽上传**(拖到列表或目录行;拖文件行到目录行=移动)、**下载**(浏览器落盘);编辑器**按类型分派**:JSON(实时校验 + 格式化)、Markdown(预览/编辑切换)、CSV/TSV(表格)、图片(预览)、**Jupyter notebook**(渲染 cells / markdown / 输出 / 内嵌图片,可切「以源码编辑」)、其余按文本(⌘S 保存)。新增端点 `/api/rw/download`(以附件流回浏览器)与 `/api/rw/upload`(请求体原始字节**流式**写入 SFTP,不走 JSON、不限大小) | 二进制往返实测:随机 3000 字节文件经面板 API 上传后远程 md5 一致、下载回来 md5 一致;UI 实际走的接口序列(列/读/写/重命名/删)对新机全部通过;notebook 读回为合法 JSON(7 cells, nbformat 4);页面元素与内联 JS 语法均校验通过 |
| 2026-09-11 | `dsh-remote/rw.mjs` | 新增 `read --out <本机文件>`:原样落盘、**不再附加** `[N 字节]` 脚注。此前只能靠 `read > file` 重定向,会被脚注污染(实测下载回来的 md5 与远程不一致) | 二进制往返三方 md5 一致(上传 `write --from` 同样验过);文本模式不变(给人看时仍带字节数脚注) |
| 2026-09-11 | 把「云上克隆实例」当成「换端点」 | 记住三件事:①端点写在**三处**(清单 `conn` / 分区挂载配置 / ssh 别名),少改一处就是半通状态 —— 用 `repoint.mjs` 一次改全;②**必须等克隆跑完再验证**,中途探测会看到空目录而误判成数据丢失(实测克隆中 6 个项目目录是空的,跑完后各项目体积与旧机账面一致);③实例重启(含切 GPU 计费)后 **SSH 端口通常会变**,需要重新 repoint |
| 2026-09-11 | 确认「原生侧栏文件树能上传/下载吗」 | **原生那棵树不能,且是设计如此**:它的界面文案只有浏览与刷新,宿主 API `dsh-api-workspace-files` 明确写着 *exposes no mutation operation*;预览里的「打开方式」(open-in-app)在挂载型分区会失灵(它让**宿主机**打开那个路径,而本机只有空占位)。另:聊天框拖拽上传走 `ctx.attachments`,文件落在**本机** `<DSH_HOME>/attachments/`(图片内联给模型,其他文件只给一个只读路径 —— 而该路径在挂载点之外,会被远程 fs 拒绝)。**但这不等于侧栏封闭** —— 见下一条 |
| 2026-09-11 | `plugins/dsh-remote-files`(新增) | **把可写远程文件树做进工作台右侧栏**。官方右侧栏是可扩展的:`ctx.sidebarRightTabs.register({id,kind,priority:'extension',title,guide})` + `ctx.slots.register({name:'sidebar.right.pane.tab',key:<同一 id>}, Body)`;文档明确 `extension` 档是"产品外部类型"且可接管内建类型。本插件注册为右侧栏的**页面型标签**,内含:懒加载目录树、新建文件/文件夹、重命名、删除、**拖拽上传**、**下载**、编辑保存(⌘S),编辑器按扩展名分派(JSON 校验+格式化 / Markdown 预览 / CSV 表格 / 图片预览 / Jupyter notebook 渲染 cells 与输出 / 其余文本)。数据走面板 HTTP API,机器身份按**当前工作台端口**反查(whoami),故同一插件装进任何分区都对上自己那台 | 客户端半边进入客户端插件图(3090 实例页面里该包名命中 1 次)、工作台正常启动、组合树条目就位、两个半边语法校验通过。**UI 的实际点击未验证(需人眼)** |
| 2026-09-11 | 判定「官方侧栏能不能扩展」 | **先前答错**:把"某个组件只读"(`dsh-client-ui-sidebar-files` 确实只读)当成了"侧栏封闭"。实际官方**专门留了扩展口子** —— 标签类型注册表分 `extension` / `builtin` / `fallback` 三档,外部包可注册 `extension` 档,并可**接管**内建类型(扩展在则扩展生效,卸载后内建自动恢复)。教训:判断"能不能扩展"要看**注册表/接缝**,不要只看某个具体实现的能力 || 2026-09-11 | `plugins/dsh-remote-files/lib/client.js` | 修复侧栏标签渲染出「📁 [object Object]」:面板端点 `/api/rw/whoami` 回的 `machine` 是**对象**(`{name,label,host,port,user,defaultPath}`)而不是字符串,客户端直接拼进模板串就成了 `[object Object]`。改为派生 `const mname = machine ? machine.name : null`,标题用 `machine.label \|\| machine.name`(无机器时回落「远程文件」),初始目录用 `defaultPath`;下载/上传查询串统一走 `mname`(修掉一次同名遮蔽) | 从**运行中的实例**取回它实际服务出去的那份 bundle(页面里带 `rev=` 的合并 URL,HTTP 200 / 23660 字节)核对:`machine.label` 1 处、`const mname` 1 处、旧的字符串拼接写法 0 处 —— 即验的是"实例吐出来的代码"而非"我磁盘上的文件";面板重启实例后 `:3090 可达`。教训:跨半边传结构化数据,拼字符串前先确认字段是标量还是对象 |
| 2026-09-11 | `dsh-remote/lib/scope.mjs`(新增)· `panel.mjs` · `panel.html` · `plugins/dsh-remote-files` | **修一个真权限漏洞**:面板的 `/api/rw/*` 原来把请求里的绝对路径原样交给 SFTP,于是「能打开面板页面」= 「能读写远程整台机器」—— 而且这条路径**完全绕开** fs provider 上那三档权限(那三档只围栏模型的文件工具)。现在按调用方分两种范围,并在**服务端**强制(客户端给的路径一律不信任):带 `port` → 该工作台**注册的工作区**(侧栏插件走这条,最窄);带 `name` → 该机器声明的**挂载根**(面板自己的文件管理器;是 `remoteRoot` 而不是 `/`)。围栏先**解 symlink 再判**:从目标往上找最深的已存在祖先做 `realpath`;末尾若本身是软链则 `readlink` 后按目标重判 —— 否则悬空软链会因 realpath 失败而回退成「在范围内」,写入时 OS 仍会顺着链落到外面。另加两条:`mv` 两端都过围栏(否则「范围内→范围外」就是把文件搬出工作区的路)、拒绝删/移范围根本身。侧栏树与面板树都改为从范围根起算、面包屑不再有 `/`,界面选择上不存在通向范围外的路 | ①纯逻辑单测 10 条:工作区根/子路径放行;同前缀兄弟目录(`.../projA-x`)、工作区之父、挂载根、`/`、`/etc/passwd`、`/root/.ssh/id_rsa`、`../` 穿越全部拒绝(`withinRoots` 用 `relPathUnder` 而非字符串前缀,所以 `ws` 不会误放 `wsx`)。②对**活机器**打矩阵:工作台范围下工作区 200、其余全 403;机器范围下挂载根与其下的子目录 200、`/`、`/etc`、`/root/../etc` 403。③在远程工作区里造四类软链实测:→`/etc`、相对 `../../../../etc`、**悬空软链**、→范围内目录 —— 前三类 403,第四类正确放行;悬空软链下的 `write` 也 403。④门外残留远程核对:`/root/pwned.txt`、`/root/stolen`、`/etc/pwned-dir`、`/tmp/...` 全部不存在。⑤范围内的 write/read/download/upload 仍全通,上传内容远程逐字节核对一致。⑥生产面板与测试面板两条路径分别验过,无 `port`/`name` 的请求直接 400。另:`/api/rw/whoami` 现在回 `scope`/`roots`/`titles`,`/api/rw/machines` 带上 `roots` |
| 2026-09-11 | `plugins/dsh-remote-files` | **修「一个工作区里并排两棵文件树」**。根因是侧栏标签注册表**按 `kind` 分槽**:`RANKS = {extension:3, builtin:2, fallback:1}`,`coexists()` 只在**同 kind、不同档**时允许并存并让高档次 shadow 低档次;而 `active()` 每个 kind 只取一个。我当初把 kind 写成了自己的 `dsh-remote-files`,官方文件树是 `files` —— **不同 kind 各占一槽,谁也 shadow 不掉谁**,于是右侧栏同时出现官方只读树与本可写树。改为 `kind: "files"` + `priority: "extension"`,按设计接管官方那棵:每个工作区只有一个文件面板(可写的),停用本插件官方自动恢复(`leave()` 里被 shadow 的 builtin 会复位)。`id` 仍用 `dsh-remote-files`(必须唯一,且标签正文按 `definition.id` 查)—— 正文插槽 key 因此不变 | 与官方包对读源码确认:`FILES_KIND = "files"`、`FILES_ID = "@deepseek-ai/dsh-client-ui-sidebar-files"`,我的 id 与之不撞;`register()` 的 `coexists(builtin, extension)` 为真 → 走 shadow 分支而非抛错。从运行中的实例取回它实际服务出去的 bundle(带 `rev=` 的 URL,HTTP 200 / 28001 字节)核对:`kind = "files"`、`state.byId[props.sessionId]`、`homeRoot` 就位,旧的 `roots.map` 为 0 |
| 2026-09-11 | `plugins/dsh-remote-files` · `dsh-remote/lib/scope.mjs` · `panel.mjs` | 侧栏的根改为**跟着本会话的工作区**,不再是「这台机器」也不是「这个工作台的全部工作区」。来源与官方侧栏文件树完全一致:插槽交给面板体的标准 prop `useSessions`/`sessionId`,`const cwd = useSessions((s) => s.byId[sessionId]?.cwd)`;客户端把 `cwd` 报给面板,面板按该工作台的挂载把它映射成远程拼写,**再对着允许的 roots 校验一次**(所以 `cwd` 只决定"显示哪个根",改不了能访问到哪),返回 `focus`。树、面包屑、刷新都只围绕这一个根。`useSessions` 是否被传入在挂载时就定了,为守住 hooks 规则拆成三个组件:分派层 → 调 hook 层 → 真正实现层 | 对活面板验:两个工作区各自的 `cwd` 分别映射到对应的远程根(`focus` 正确)、`defaultPath` 跟随;`/etc` 与本机无关路径映射不出 → `focus` 为 null 并回落到允许集第一个。运行实例的 bundle 里 `state.byId[props.sessionId]` 与 `homeRoot` 均在,`roots.map` 为 0 |
| 2026-09-11 | 判定「DSH 又更新了、右侧栏重做了」 | **不是新版本**。npm registry dist-tags 仍是 `alpha=0.1.5-alpha.2 / next=0.1.5-rc.2 / latest=0.1.5-rc.1`,最新发布 `0.1.5-rc.2`(2026-09-10T14:57);GitHub releases atom 里最新 tag 也是 `dsh-v0.1.5-rc.2`,其自身更新只有反馈弹窗与文件卡片排版。**「右侧栏重做」指的就是 0.1.5 这一版的新功能**:`v0.1.5-alpha.1` 新增实验性右侧 Sidebar(多标签 / 分栏 / 全屏)并移除原 Detail 面板,`v0.1.5-rc.1` 补齐 Markdown/代码/HTML/PDF/图片预览、子代理与未激活会话的文件、模型显式交付文件。本机 `harness/state.json` 的 `current` 早已是 `0.1.5-rc.2`(previous `0.1.2-rc.1`,而 0.1.2 里根本没有 `dsh-client-ui-sidebar-right` 这个包)—— 即一直都是这个新侧栏 | `curl registry.npmjs.org/@deepseek-ai%2Fdsh` 读 dist-tags 与发布时间;`releases.atom` 读最新 tag 与正文;`harness/state.json` 与 `ls harness/` 核对已装版本;`bin/dsh` shim 确认指向 `harness/current` |
| 2026-09-11 | `plugins/dsh-remote-files` | **撤回上一条的「接管官方文件树」**。用户要官方那棵,已改回自己的 kind(`dsh-remote-files`),官方 `files` 树恢复生效。同时记清一条容易搞反的语义:**`priority` 管不了"并存"** —— 它只在**同一个 kind 内部**决定谁 shadow 谁(RANKS 3/2/1);**不同 kind 必然各占一槽、同时出现**。所以"别让官方那棵出现"的唯一办法是本插件**不注册标签**,而不是把自己调低档或去抢 `files`。之前把 kind 改成 `files` 虽然确实做到了"只剩一个文件面板",但代价是官方只读树被顶掉,不是用户要的 | 改回后从运行实例取回 bundle(rev 4982067acc86,27936 字节)核对:`TAB_KIND = "dsh-remote-files"` 就位、旧的 `TAB_KIND = "files"` 为 0;本会话工作区逻辑(`state.byId[props.sessionId]`)保留 |
| 2026-09-13 | 3090 工作台的 profile | **按用户要求把 `dsh-remote-files` 从 3090 卸掉**,只留官方那棵只读文件树。挂载点在该工作台自己的 profile 上两处:`profiles/web/package.json` 的 `dependencies`(link:)与 `dsh.profile.bundles` 列表 —— 两处都删才干净(只删 bundles 会留下悬空 link,只删依赖会因 bundles 里仍列着而在重装时被拉回)。插件代码保留在 `~/.dsh/plugins/` 与仓库里作为留档(与 `dsh-remote-ui` 同样处理),只是不再被加载;`workbench.mjs` 的 `DEFAULT_PLUGINS` 本来就只有那 4 个 provider,**新建工作台不会带上它** | 重启实例后核对:客户端插件图里 `dsh-remote-files` 命中 0、`ui-sidebar-files`/`ui-sidebar-right` 仍在;`--dump-config` 157 个条目里 `dsh-remote-files` 命中 0,而四个接缝 provider 都在(`fs-sftp`/`subprocess-sftp`/`bash-sftp`/`directory-picker-sftp` 四个条目 `name:` 分别指向它们,patch 注释链完整),`bash-sandbox`/`fs-sandbox`/`directory-picker` 三个官方 provider 仍是 `disabled`;启动日志无 error 行、`:3090` 端口就绪 |
| 2026-09-13 | 一个无法兼得的需求,留档定论 | **「官方侧栏文件树」与「侧栏里上传/下载」不能同时成立在同一个面板里**。官方那棵树的宿主 API(`dsh-api-workspace-files`)只提供读与目录观察,注释里明确 *exposes no mutation operation*;而标签注册表只能**整个 kind** 换掉,没法把按钮塞进官方树里(它的正文是官方包自己的组件)。可选的只有三条:①官方树 + 另加一个可写标签(侧栏两个文件入口);②官方树浏览 + 用「资源型标签」(`patterns` + `extension`)接管点开文件的行为,换成自己的可写编辑器(图片/PDF/Markdown 仍走官方预览);③只要官方树,放弃侧栏上传/下载,改用面板 `:4100` 的「📁 文件」。用户选了 ③ —— 需要上传/下载时走面板那个(已加访问范围围栏) | 结论来自读 `dsh-client-ui-sidebar-files` 与 `dsh-client-ui-sidebar-right` 的源码,不是猜的 |
| 2026-09-13 | 克隆实例后核对磁盘占用 | `df` 说数据盘用了 96G,而 `du -sh` 逐项加起来只有 60 出头 —— 差的 35G 在 **`.Trash-0`**:数据盘自带的回收站被一并克隆过来了。三件事:①对不上账时先 `du -sh <dir>/.[!.]*`(glob `*` 看不到隐藏项),再比 `du -sh <整个目录>` 与 `df` 是否自洽,别急着怀疑丢数据;②克隆会把回收站一起搬,新机上先清它比事后找空间划算;③判定「克隆是否还在写」用 `df` 前后两次采样就够,比 `du` 轻得多 —— 大目录树上 `du` 要走全树,克隆期间等于自己加 I/O |
| 2026-09-13 | 又一次 repoint(实例重启换端口)| 同一个实例重启后端口会变,第二次 repoint 只改了端口。**一个值得记住的判据**:换端口后 `hostname`(容器 id)与内核补丁号**都可能变**(实测 `…d5b344ad43` / 5.15.0-143 → `…0f324fa4a3` / 5.15.0-78),但**数据盘是同一块**(96G/130G/74% 与两个工作区体积逐项一致)。所以「是不是同一台机」看**数据盘与账面**,别看容器 id —— 反过来,上次克隆进行中我也是拿「目录是空的」当成了「换机器了」。两次 repoint 都在跑前先探端点可达性 + 核账面,再动手 |
| 2026-09-13 | 第三次 repoint(又换端点,host 与端口都变)| 同一天第三次,这次 host 与端口都变。数据盘仍是同一块(占用与占比都没动),两个工作区逐项对得上,其中一个还在持续增长(用户一直在里面干活)。**结论升级**:端点(host、端口、容器 id)全是易变的,唯一稳定的锚是**数据盘 + 账面**;已把「先探可达性 → 按账面核数据 → repoint --ssh-config → 面板重启 → 验两条接缝的连接路径」固化成五步写进 `skills/gpu-partition/SKILL.md`。三次 repoint 的数据盘占用完全一致,说明每次变的只是容器层 |
