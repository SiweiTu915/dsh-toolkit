# 依赖与运行产物
node_modules/
*.log
.DS_Store

# 个人 / 机密：永不提交(若把本仓库直接建在 DSH_HOME 里,这些规则同样适用)
.credentials.yaml
.anonymous-user-id
settings.yaml
sessions/
storages/
backups/
harness/
.npm-cache/
.migrate-tmp/
.update-tmp-*/
.migrate-manifest.json

# dsh-remote 个人配置(提交用 servers.example.json)
dsh-remote/servers.json
dsh-remote/.state/
dsh-remote/sim-homes/

# 本地镜像目录(含远程代码,绝不提交)
dsh-remote/mirrors/
